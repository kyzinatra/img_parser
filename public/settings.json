{
	"mode": "random", // ["random", "sequentially"] (How should the path to the image be created (in 36-bit format))
	"length": [5, 6], // [from, to] or number, Path length to the image (by default from 4 to 6 (randomly or not, depends on mode))
	"filterBadPaths": true, // On true doesn't display an error in the console about an invalid path
	"log": true, // If true, it outputs the link and successful results to the console
	"startsFrom": null, // Indicates which path the program should start with (when mode: sequentially!)
	"streams": 2 // from 1 to +Infinity that's how many streams program will be use.
	//! WARNING DO NOT WRITE MORE THAN 3 YOU MAY BE BANNED !!!
}
